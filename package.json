{
  "name": "sniffies-soft-filter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Dev tooling + interaction library for the Sniffies Soft Filter userscript. The userscript itself ships as a single .txt and is dependency-free; the version of record lives in its @version header, not here.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:cov": "vitest run --coverage",
    "test:lib": "node --test 'test/lib/*.test.mjs'",
    "lint": "eslint .",
    "build:lib": "node scripts/build-lib.mjs",
    "build:check": "node scripts/build-lib.mjs && git diff --exit-code -- dist/",
    "check": "npm run lint && npm test && npm run test:lib && npm run build:check"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.9",
    "eslint": "^10.5.0",
    "globals": "^17.6.0",
    "jsdom": "^30.0.1",
    "vitest": "^4.1.9"
  }
}
