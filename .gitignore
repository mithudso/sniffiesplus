node_modules/
coverage/
*.bak
.DS_Store
*.sublime-workspace
# iCloud/Finder duplicate-on-conflict copies (e.g. "sniffiesplus 2.js") — stale, not source.
* [0-9].js
* [0-9].txt
# Legacy version-in-filename snapshots and the old .txt variants, kept on disk if present but
# never tracked — git history is the source of truth for prior versions (the userscript is now
# the single sniffiesplus.js).
Sniffies Soft Filter (Bottom*.txt
sniffiesplus.txt
# Sniffies' own production app bundle (MapLibre GL, their Angular build, Chargebee JS, fonts) —
# saved locally as a reverse-engineering reference for docs/sniffies-dom-and-api.md. It's their
# compiled/third-party code, not ours to redistribute — kept on disk, never pushed publicly.
Sniffies App _ Map.html
Sniffies App _ Map_files/
# Claude Code local session state (worktrees, etc.) — never repo content.
.claude/
